- var note = model;
- var helper = App.ViewHelpers.notes;
%span &= note.get('name')
%div.note_links
  %a(href='#' note-id=note.id class=helper.links.edit)Edit
  |
  %a(href='#' note-id=note.id class=helper.links.destroy)Delete